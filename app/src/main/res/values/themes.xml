<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Project2" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/dark_blue</item>
        <item name="colorPrimaryVariant">@color/dark_felt</item>
        <item name="colorOnPrimary">@color/blue_grey</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/yellow2</item>
        <item name="colorSecondaryVariant">@color/blue_grey</item>
        <item name="colorOnSecondary">@color/jet_black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="Theme.App.starting" parent="Theme.SplashScreen">
        <!--Set the splash screen background, animated icon, and animation duration.-->
        <item name="windowSplashScreenBackground">@color/dark_felt</item>
        <!-- Use windowSplashScreenAnimatedIcon to add either a drawable or an
        animated drawable. One of these is required-->
        <item name="windowSplashScreenAnimatedIcon">@drawable/poker_icon1</item>
        <item name="windowSplashScreenAnimationDuration">300</item> <!--# Required for-->
        <!--# animated icons-->
        <!--Set the theme of the activity that directly follows your splash screen-->
        <item name="postSplashScreenTheme">@style/Theme.Project2</item>
    </style>
</resources>